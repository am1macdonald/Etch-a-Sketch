let gameGrid = document.getElementById("game-grid");

function makeGrid(num) {
    let gridItem = document.createElement('div');
    gridItem.clasList.add('grid-item');
    for(let i = 0; i < num; i++) {
        document.getElementById("game-grid");

    }
}